name: Make CI

on: [push]

jobs:
  Compile:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: test
      run: cd src && make pgo
    - name: release
      run: cd src && make release
    - name: sanitize
      run: cd src && make pgo CC=clang
